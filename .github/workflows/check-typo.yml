# workflows/check-typo.yml
#
# Check Typo
# Check Typo using codespell.

name: Check Typo

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

concurrency:
  group: check-typo-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check-typo:
    name: Check Typo using codespell
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false

    steps:
      - name: Checkout Git Repository
        uses: actions/checkout@v7

      - name: Check Typo
        uses: paradedb/actions/check-typo@a024855293e56a720daa69e6648d023dd0cb9f77
        with:
          ignore-words-file: .codespellignore
          skip: package-lock.json,pnpm-lock.yaml,*.svg,metadata.json
