I like the current blog post in index.md, but I want to change some things. 

- keep the intro, I want to talk about my role and what we are building
- dive into what we think benchmark runners get wrong
  - benchmark tooling built with benchmark, like ClickBench. While this provides simpicity, it also ends up with benchmarks that aren't really real world applicabale.
  - no ability to simulate real world load, multiple queries
  - target a single database
  - target multiple databases, but poorly tuned on all but one
  - no ability to capture setup with runs

You can also suggest other options.

Then go into whatr we built and how it works.


